# 🚀 Инструкция по деплою сайта

## Вариант 1: Render.com (РЕКОМЕНДУЕТСЯ) ⭐

**Плюсы:** Бесплатно, поддерживает Python, простая настройка

### Шаги:

1. **Зарегистрируйтесь на [Render.com](https://render.com)** (бесплатно)

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Или загрузите код напрямую

3. **Настройки:**
   ```
   Name: wedding-site
   Environment: Python 3
   Build Command: pip install -r requirements.txt
   Start Command: cd admin && python server.py
   ```

4. **Переменные окружения (если нужно):**
   - Не требуются для базовой настройки

5. **После деплоя:**
   - Ваш сайт будет доступен по адресу: `https://your-app.onrender.com`
   - Админка: `https://your-app.onrender.com/admin/dashboard.html`

---

## Вариант 2: Railway.app

**Плюсы:** Бесплатно, очень просто, автоматический деплой

### Шаги:

1. **Зарегистрируйтесь на [Railway.app](https://railway.app)**

2. **Создайте проект:**
   - "New Project" → "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте Railway:**
   - Railway автоматически определит Python
   - Start Command: `cd admin && python server.py`
   - Port: Railway назначит автоматически

4. **Готово!** Ваш сайт будет доступен по адресу от Railway

---

## Вариант 3: GitHub Pages + отдельный сервер для API

**Плюсы:** GitHub Pages бесплатно для статики, но нужен отдельный сервер для API

### Шаги:

1. **Для статики (GitHub Pages):**
   - Загрузите файлы сайта в репозиторий
   - Settings → Pages → выберите ветку
   - Сайт будет на: `https://yourusername.github.io/svadba/`

2. **Для API (используйте Render/Railway):**
   - Задеплойте только папку `admin/` на Render
   - Обновите URL в `tloader.html` на адрес вашего API сервера

---

## Вариант 4: Vercel (Node.js версия)

Нужно переписать сервер на Node.js. Создам отдельную инструкцию если нужно.

---

## 📋 Что нужно подготовить:

### 1. Создайте файл `requirements.txt`:

```txt
# Пустой файл - все библиотеки встроены в Python
```

### 2. Создайте файл `Procfile` (для Heroku/Render):

```
web: cd admin && python server.py
```

### 3. Обновите `server.py` для работы на разных портах:

```python
PORT = int(os.environ.get('PORT', 8000))
```

---

## 🔧 Быстрый деплой на Render:

1. **Создайте аккаунт на Render.com**

2. **Создайте новый Web Service**

3. **Настройки:**
   - **Name:** `wedding-site`
   - **Environment:** `Python 3`
   - **Build Command:** `echo "No build needed"`
   - **Start Command:** `cd admin && python server.py`
   - **Port:** Оставьте пустым (Render назначит автоматически)

4. **Нажмите "Create Web Service"**

5. **Готово!** Ваш сайт будет доступен по адресу типа:
   - `https://wedding-site.onrender.com`
   - Админка: `https://wedding-site.onrender.com/admin/dashboard.html`

---

## ⚠️ Важные замечания:

1. **База данных SQLite** будет храниться в `/tmp` на сервере
   - Данные могут теряться при перезапуске сервера
   - Для продакшена лучше использовать PostgreSQL (Render предоставляет бесплатно)

2. **Статические файлы** должны быть доступны
   - Убедитесь что все файлы загружены в репозиторий

3. **CORS** уже настроен в коде

---

## 🎯 Рекомендация:

**Используйте Render.com** - это самый простой и надежный вариант для Python приложений!
