# 🚀 Деплой на Render

## Проблема
Render пытается использовать Node.js вместо Python, из-за чего возникает ошибка `ModuleNotFoundError: No module named 'psycopg2'`.

## ✅ Решение

### 1. Настройки в Render Dashboard

1. Зайдите в ваш проект на Render
2. Перейдите в **Settings**
3. Найдите раздел **Build & Deploy**

**Build Command:**
```
pip install -r requirements.txt
```

**Start Command:**
```
cd admin && python server.py
```

**Environment:**
- Выберите **Python 3**

### 2. Переменные окружения

В разделе **Environment Variables** добавьте:

```
DATABASE_URL=postgresql://postgres.dxfnguweggcefslzgvzs:tQ/5ShuHtg7FbgT@aws-1-eu-central-1.pooler.supabase.com:6543/postgres
PORT=10000
```

**Важно:** 
- `PORT` должен быть `10000` (Render автоматически назначает порт, но лучше указать явно)
- `DATABASE_URL` - ваша строка подключения из Supabase

### 3. Файлы в проекте

Убедитесь, что в корне проекта есть:
- ✅ `requirements.txt` с `psycopg2-binary>=2.9.0`
- ✅ `Procfile` с `web: cd admin && python server.py`
- ✅ `runtime.txt` с версией Python (опционально)

### 4. После настройки

1. Сохраните изменения в Render
2. Нажмите **Manual Deploy** → **Deploy latest commit**
3. Дождитесь завершения деплоя

### 5. Проверка

После успешного деплоя:
1. Откройте ваш сайт на Render
2. Попробуйте заполнить форму
3. Проверьте админку: `https://your-app.onrender.com/admin/dashboard.html`

---

## 🔍 Если все еще не работает

### Проверьте логи в Render:
1. Откройте **Logs** в Render Dashboard
2. Найдите ошибки при деплое
3. Убедитесь, что `pip install -r requirements.txt` выполняется успешно

### Альтернативный Build Command:
Если стандартный не работает, попробуйте:
```
python -m pip install --upgrade pip && pip install -r requirements.txt
```

### Проверьте структуру проекта:
```
svadba/
├── requirements.txt  ← должен быть в корне
├── Procfile         ← должен быть в корне
├── runtime.txt      ← опционально
├── admin/
│   └── server.py
└── wedding_files/
    └── tloader.html
```
