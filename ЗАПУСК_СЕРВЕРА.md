# 🚀 Как запустить сервер локально

## Шаг 1: Установить зависимости

Откройте терминал в папке проекта и выполните:

```powershell
cd c:\Users\UserVik\Desktop\code\svadba
pip install -r requirements.txt
```

Или если `pip` не работает, попробуйте:
```powershell
python -m pip install -r requirements.txt
```

Это установит библиотеку `psycopg2-binary` для работы с PostgreSQL.

---

## Шаг 2: Установить переменную окружения DATABASE_URL

### Вариант А: Через PowerShell (для текущей сессии)

```powershell
$env:DATABASE_URL = "postgresql://postgres:tQ/5ShuHtg7FbgT@db.dxfnguweggcefslzgvzs.supabase.co:5432/postgres"
```

### Вариант Б: Через системные переменные (постоянно)

1. Нажмите `Win + R`
2. Введите `sysdm.cpl` и нажмите Enter
3. Вкладка "Дополнительно" → "Переменные среды"
4. В "Переменные пользователя" нажмите "Создать"
5. Имя: `DATABASE_URL`
6. Значение: `postgresql://postgres:tQ/5ShuHtg7FbgT@db.dxfnguweggcefslzgvzs.supabase.co:5432/postgres`
7. Нажмите OK

**После установки через системные переменные перезапустите терминал!**

---

## Шаг 3: Запустить сервер

### Способ 1: Из папки admin

```powershell
cd c:\Users\UserVik\Desktop\code\svadba\admin
python server.py
```

### Способ 2: Из корня проекта

```powershell
cd c:\Users\UserVik\Desktop\code\svadba
cd admin
python server.py
```

### Способ 3: С указанием порта (если 8000 занят)

```powershell
cd c:\Users\UserVik\Desktop\code\svadba\admin
$env:PORT = "8080"
python server.py
```

---

## Шаг 4: Открыть сайт

После запуска сервера вы увидите сообщение:

```
============================================================
SERVER STARTED!
Port:        8000
Database:    PostgreSQL (Supabase)
Site:        http://localhost:8000/index.html
Admin panel: http://localhost:8000/admin/dashboard.html
Stop:        Ctrl+C
============================================================
```

Откройте в браузере:
- **Сайт:** http://localhost:8000/index.html
- **Админка:** http://localhost:8000/admin/dashboard.html

---

## ⚠️ Возможные проблемы

### Ошибка: "DATABASE_URL не установлена"

**Решение:** Убедитесь, что переменная окружения установлена:
```powershell
echo $env:DATABASE_URL
```

Если пусто, установите её (см. Шаг 2).

### Ошибка: "No module named 'psycopg2'"

**Решение:** Установите зависимости:
```powershell
pip install psycopg2-binary
```

### Ошибка: "Port 8000 is already in use"

**Решение:** Используйте другой порт:
```powershell
$env:PORT = "8080"
python server.py
```

### Ошибка подключения к базе данных

**Решение:** 
1. Проверьте, что DATABASE_URL правильный
2. Проверьте интернет-соединение
3. Убедитесь, что Supabase база данных активна

---

## 🛑 Остановка сервера

Нажмите `Ctrl + C` в терминале, где запущен сервер.

---

## 📝 Быстрый старт (все команды сразу)

```powershell
# Перейти в папку проекта
cd c:\Users\UserVik\Desktop\code\svadba

# Установить зависимости (только первый раз)
pip install -r requirements.txt

# Установить переменную окружения (для текущей сессии)
$env:DATABASE_URL = "postgresql://postgres:tQ/5ShuHtg7FbgT@db.dxfnguweggcefslzgvzs.supabase.co:5432/postgres"

# Запустить сервер
cd admin
python server.py
```

---

## 🌐 Для деплоя на Render

На Render всё уже настроено через `Procfile` и переменные окружения в панели управления. Просто задеплойте код - сервер запустится автоматически!
