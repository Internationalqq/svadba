# 🔧 Настройка PostgreSQL на Render

## Проблема
На бесплатном плане Render сервис "засыпает" после неактивности, и файловая БД SQLite теряется при перезапуске. Поэтому данные пропадали.

## Решение
Переключили на PostgreSQL (Supabase) - данные теперь хранятся в облаке и не теряются.

---

## 📋 Шаги настройки на Render

### 1. Добавьте переменную окружения DATABASE_URL

1. Зайдите на [Render Dashboard](https://dashboard.render.com/)
2. Выберите ваш Web Service
3. Перейдите в раздел **Environment**
4. Нажмите **Add Environment Variable**
5. Добавьте:
   - **Key:** `DATABASE_URL`
   - **Value:** `postgresql://postgres:tQ/5ShuHtg7FbgT@db.dxfnguweggcefslzgvzs.supabase.co:5432/postgres`
6. Нажмите **Save Changes**

### 2. Перезапустите сервис

После добавления переменной:
1. Перейдите в раздел **Events**
2. Нажмите **Manual Deploy** → **Deploy latest commit**
3. Или просто подождите - Render автоматически перезапустит сервис

---

## ✅ Проверка

После перезапуска:
1. Откройте админ-панель: `https://ваш-домен.onrender.com/admin/dashboard.html`
2. Попробуйте заполнить форму на сайте
3. Проверьте, что данные сохраняются и отображаются в админке

---

## 🔍 Если что-то не работает

### Проверьте логи на Render:
1. В разделе **Logs** посмотрите ошибки
2. Должно быть сообщение: `Database connection successful!`

### Проверьте переменную окружения:
- Убедитесь, что `DATABASE_URL` добавлена правильно
- URL должен начинаться с `postgresql://`

### Проверьте подключение к Supabase:
- Зайдите на [Supabase Dashboard](https://app.supabase.com/)
- Проверьте, что база данных активна
- В разделе **Table Editor** должна быть таблица `responses`

---

## 📝 Что изменилось

- ✅ Используется PostgreSQL вместо SQLite
- ✅ Данные хранятся в облаке (Supabase)
- ✅ Данные не теряются при перезапуске сервера
- ✅ Работает даже когда сервер "спит" (данные сохраняются в БД)

---

## 🆘 Если данные всё равно не сохраняются

1. Проверьте логи на Render - там будут ошибки подключения к БД
2. Убедитесь, что переменная `DATABASE_URL` установлена правильно
3. Проверьте, что Supabase база данных активна
4. Попробуйте пересоздать таблицу - она создастся автоматически при первом запросе
