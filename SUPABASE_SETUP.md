# 🔧 Настройка Supabase для подключения

## Проблема
Подключение к Supabase PostgreSQL не работает из-за таймаута. Это может быть связано с настройками безопасности в Supabase.

---

## ✅ Что нужно проверить и настроить в Supabase

### 1. Проверьте, что база данных активна

1. Зайдите на [Supabase Dashboard](https://app.supabase.com/)
2. Выберите ваш проект
3. Убедитесь, что проект **активен** (не в режиме паузы)

---

### 2. Получите правильный Connection String

В Supabase есть **два типа** строк подключения:

#### Вариант А: Direct Connection (прямое подключение)
- Используется для прямого подключения к PostgreSQL
- Формат: `postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres`
- **Может не работать** из-за настроек безопасности

#### Вариант Б: Connection Pooling (рекомендуется!)
- Используется для подключений через пул соединений
- Более надежный и быстрый
- Формат: `postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres`
- **Порт 6543** вместо 5432

---

### 3. Как получить Connection String в Supabase

1. Зайдите в ваш проект на [Supabase Dashboard](https://app.supabase.com/)
2. Перейдите в **Settings** → **Database**
3. Найдите раздел **Connection string**
4. Выберите вкладку **Connection pooling** (Transaction mode)
5. Скопируйте строку подключения

Она будет выглядеть примерно так:
```
postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

**Важно:** Используйте порт **6543** (connection pooling), а не 5432!

---

### 4. Обновите DATABASE_URL

После получения правильной строки подключения:

1. Обновите файл `.env` в корне проекта:
   ```
   DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
   ```

2. Или установите переменную окружения при запуске сервера:
   ```powershell
   $env:DATABASE_URL = "postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres"
   ```

---

### 5. Проверьте настройки безопасности (если все еще не работает)

В Supabase Dashboard:

1. **Settings** → **Database**
2. Проверьте раздел **Connection pooling**
3. Убедитесь, что **Connection pooling** включен
4. Проверьте **IP Restrictions** (если есть) - возможно, нужно добавить ваш IP

---

### 6. Альтернатива: Используйте Supabase API вместо прямого подключения

Если прямое подключение не работает, можно использовать Supabase REST API или клиентскую библиотеку. Но для вашего случая (Python сервер) лучше использовать connection pooling.

---

## 🔍 Как проверить, что подключение работает

Запустите тестовый скрипт:

```powershell
cd c:\Users\UserVik\Desktop\code\svadba\admin
python test_db_connection.py
```

Если подключение успешно, вы увидите:
```
✅ Connection object created successfully!
✅ Test query executed successfully: (1,)
✅ Connection closed successfully!
🎉 Database connection test PASSED!
```

---

## 📝 Пример правильной строки подключения

**Неправильно (прямое подключение, может не работать):**
```
postgresql://postgres:tQ/5ShuHtg7FbgT@db.dxfnguweggcefslzgvzs.supabase.co:5432/postgres
```

**Правильно (connection pooling, рекомендуется):**
```
postgresql://postgres.dxfnguweggcefslzgvzs:tQ/5ShuHtg7FbgT@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
```

Обратите внимание на:
- `postgres.[PROJECT-REF]` вместо `postgres`
- `aws-0-[REGION].pooler.supabase.com` вместо `db.[PROJECT-REF].supabase.co`
- Порт **6543** вместо 5432

---

## 🆘 Если все еще не работает

1. Проверьте логи сервера - там будет видно точную ошибку
2. Убедитесь, что проект Supabase не на паузе
3. Проверьте, что пароль правильный
4. Попробуйте подключиться через другой инструмент (например, pgAdmin) для проверки
5. Проверьте файрвол/антивирус - они могут блокировать подключение

---

## 💡 Рекомендация

**Используйте Connection Pooling URL** - это самый надежный способ подключения к Supabase из внешних приложений!
