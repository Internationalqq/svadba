# 📋 Система сбора ответов гостей

## 🚀 Как запустить

### Вариант 1: Локальный PHP сервер (проще всего)

1. **Убедитесь что установлен PHP** (обычно идет с XAMPP, OpenServer, Denwer)
   
2. **Откройте терминал в папке проекта** и выполните:
   ```bash
   php -S localhost:8000
   ```

3. **Откройте в браузере:**
   - Сайт: `http://localhost:8000/Сайтмейкер!!!.htm`
   - Админ-панель: `http://localhost:8000/admin/`

### Вариант 2: Через XAMPP/OpenServer

1. Скопируйте всю папку `svadba` в:
   - **XAMPP**: `C:\xampp\htdocs\`
   - **OpenServer**: `C:\OpenServer\domains\`

2. Откройте в браузере:
   - Сайт: `http://localhost/svadba/Сайтмейкер!!!.htm`
   - Админ-панель: `http://localhost/svadba/admin/`

### Вариант 3: Загрузить на хостинг

1. Загрузите все файлы на хостинг через FTP
2. Убедитесь что на хостинге есть PHP 7.4+
3. Откройте ваш сайт и `/admin/` для просмотра ответов

---

## 📊 Как использовать

### Для гостей:
1. Гость заполняет анкету на сайте
2. Нажимает "Подтвердить"
3. Данные автоматически сохраняются в базу

### Для вас (администратора):
1. Откройте `http://ваш-сайт.ru/admin/`
2. Увидите статистику и список всех ответов
3. Нажмите **"Скачать Excel"** для экспорта в таблицу

---

## 📁 Структура файлов

```
svadba/
├── admin/
│   ├── index.php          # Админ-панель (просмотр ответов)
│   ├── save_form.php      # Сохранение данных из формы
│   ├── export_excel.php   # Экспорт в Excel
│   └── README.md          # Эта инструкция
├── wedding_responses.db   # База данных (создается автоматически)
└── Сайтмейкер!!!.htm     # Ваш сайт
```

---

## ✨ Возможности админ-панели

- ✅ Просмотр всех ответов в удобной таблице
- 📊 Статистика: кто придет, кто нет, кто поедет автобусом
- 📥 Экспорт в Excel/CSV для фильтрации
- 🔄 Автоматическое обновление данных
- 📱 Адаптивный дизайн (работает на телефоне)

---

## 🔒 Безопасность

**ВАЖНО!** Защитите админ-панель паролем:

Создайте файл `.htaccess` в папке `admin/`:
```apache
AuthType Basic
AuthName "Админ-панель"
AuthUserFile /полный/путь/к/.htpasswd
Require valid-user
```

Создайте файл `.htpasswd` с паролем:
```bash
htpasswd -c .htpasswd admin
```

---

## 🛠️ Настройки базы данных

База данных **SQLite** создается автоматически при первом запуске.
Файл: `wedding_responses.db`

### Поля в базе:
- `id` - уникальный номер
- `name` - имя гостя
- `attendance` - придет или нет
- `companion` - с кем придет
- `bus_option` - вариант поездки на автобусе
- `drinks` - предпочтения по напиткам
- `created_at` - дата и время ответа

---

## 📞 Помощь

Если что-то не работает:
1. Проверьте что PHP установлен (`php -v` в терминале)
2. Убедитесь что файл `wedding_responses.db` создался
3. Проверьте права доступа к папке (должна быть возможность записи)

---

## 🎉 Готово!

Теперь все ответы гостей будут сохраняться автоматически, и вы сможете удобно их просматривать и фильтровать в Excel!
