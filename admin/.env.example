# Скопируйте в .env на сервере: cp .env.example .env && nano .env
# Формат: одна строка, без кавычек (если в пароле есть спецсимволы — оставьте как в Supabase)
DATABASE_URL=postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@aws-1-eu-central-1.pooler.supabase.com:6543/postgres
