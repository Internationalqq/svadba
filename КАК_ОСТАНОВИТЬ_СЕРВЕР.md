# 🛑 Как остановить Python сервер

## Способ 1: Найти и остановить все процессы Python

```powershell
# Показать все процессы Python
Get-Process | Where-Object {$_.ProcessName -eq "python"}

# Остановить все процессы Python
Get-Process | Where-Object {$_.ProcessName -eq "python"} | Stop-Process -Force
```

---

## Способ 2: Найти процесс по порту и остановить

```powershell
# Найти процесс на порту 8000
netstat -ano | Select-String ":8000.*LISTENING"

# Остановить процесс по PID (замените 10148 на ваш PID)
Stop-Process -Id 10148 -Force

# Или одной командой:
netstat -ano | Select-String ":8000.*LISTENING" | ForEach-Object { 
    $pid = ($_ -split '\s+')[-1]
    Stop-Process -Id $pid -Force
}
```

---

## Способ 3: В терминале где запущен сервер

Если сервер запущен в терминале, просто нажмите:
```
Ctrl + C
```

---

## Способ 4: Через Диспетчер задач

1. Откройте **Диспетчер задач** (Ctrl + Shift + Esc)
2. Перейдите на вкладку **"Процессы"** или **"Подробности"**
3. Найдите процесс **python.exe**
4. Нажмите правой кнопкой → **"Завершить задачу"**

---

## 🔍 Проверка, что сервер остановлен

```powershell
# Проверить, что порт 8000 свободен
netstat -ano | Select-String ":8000.*LISTENING"

# Если ничего не выводится - порт свободен, сервер остановлен
```

---

## 💡 Быстрая команда (все в одной строке)

```powershell
Get-Process | Where-Object {$_.ProcessName -eq "python"} | Stop-Process -Force
```

Это остановит ВСЕ процессы Python на вашем компьютере.
