# 📋 Создание таблицы в Supabase

## Проблема
Ваш проект Supabase только что создан, и таблица `responses` еще не существует. Нужно создать её вручную.

---

## ✅ Пошаговая инструкция

### Шаг 1: Создайте таблицу

1. В Supabase Dashboard нажмите **"Create a table"** (или перейдите в **Table Editor** слева)
2. Нажмите **"New table"**
3. Заполните:
   - **Name:** `responses`
   - **Description:** (опционально) "Ответы гостей на свадьбу"
4. Нажмите **"Save"**

---

### Шаг 2: Добавьте колонки

После создания таблицы добавьте следующие колонки:

| Column Name | Type | Default Value | Nullable |
|-------------|------|---------------|----------|
| `id` | `int8` | (auto) | ❌ No |
| `name` | `text` | - | ❌ No |
| `companion` | `text` | - | ✅ Yes |
| `attendance` | `text` | - | ✅ Yes |
| `bus_option` | `text` | - | ✅ Yes |
| `drinks` | `text` | - | ✅ Yes |
| `created_at` | `timestamptz` | `now()` | ✅ Yes |

**Как добавить колонки:**

1. В таблице `responses` нажмите **"Add column"**
2. Для каждой колонки:
   - **Name:** введите имя колонки (например, `name`)
   - **Type:** выберите тип (например, `text`)
   - **Is Nullable:** отметьте галочкой, если колонка может быть пустой
   - **Default Value:** укажите значение по умолчанию (для `created_at` - `now()`)
3. Нажмите **"Save"**

**Особенности:**

- Колонка `id` должна быть **Primary Key** и **Auto-increment**
  - При создании таблицы Supabase автоматически создаст колонку `id` типа `bigint` с автоинкрементом
  - Если её нет, добавьте вручную как `int8` с галочкой "Is Primary Key"

- Колонка `created_at` должна иметь значение по умолчанию `now()`
  - При добавлении колонки в поле "Default Value" введите: `now()`

---

### Шаг 3: Проверьте структуру таблицы

После создания всех колонок таблица должна выглядеть так:

```
responses
├── id (bigint, Primary Key, Auto-increment)
├── name (text, Not Null)
├── companion (text, Nullable)
├── attendance (text, Nullable)
├── bus_option (text, Nullable)
├── drinks (text, Nullable)
└── created_at (timestamptz, Default: now())
```

---

### Шаг 4: (Опционально) Настройте Row Level Security

Если хотите защитить данные:

1. Перейдите в **Authentication** → **Policies**
2. Создайте политику для таблицы `responses`
3. Или пока оставьте без политик для тестирования

**Для тестирования можно оставить RLS выключенным.**

---

## 🚀 Быстрый способ: Используйте SQL Editor

Если хотите создать таблицу быстрее через SQL:

1. В Supabase Dashboard перейдите в **SQL Editor** (слева)
2. Создайте новый запрос
3. Вставьте следующий SQL:

```sql
CREATE TABLE IF NOT EXISTS responses (
    id BIGSERIAL PRIMARY KEY,
    name TEXT NOT NULL,
    companion TEXT,
    attendance TEXT,
    bus_option TEXT,
    drinks TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

4. Нажмите **"Run"** или `Ctrl+Enter`

---

## ✅ После создания таблицы

1. Перезапустите ваш Python сервер
2. Попробуйте заполнить форму на сайте
3. Проверьте админку - данные должны появиться

---

## 🔍 Проверка

Чтобы проверить, что таблица создана:

1. В Supabase Dashboard перейдите в **Table Editor**
2. Вы должны увидеть таблицу `responses`
3. Попробуйте добавить тестовую запись вручную через интерфейс

---

## 💡 Совет

После создания таблицы сервер автоматически будет использовать её. Код в `server.py` уже содержит логику создания таблицы (`CREATE TABLE IF NOT EXISTS`), но лучше создать её вручную в Supabase для уверенности.
