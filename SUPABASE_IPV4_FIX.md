# 🔧 Решение проблемы IPv4 в Supabase

## Проблема

Supabase показал важную информацию:
- **"Not IPv4 compatible"** - прямое подключение не работает с IPv4
- **"Use Session Pooler if on a IPv4 network"** - нужно использовать Session Pooler

Ваша сеть использует IPv4, поэтому прямое подключение на порту 5432 не работает.

---

## ✅ Решение: Использовать Session Pooler

### Шаг 1: Найдите Session Pooler URL

В том же разделе Supabase Dashboard, где вы видите "Not IPv4 compatible", должна быть опция **"Session Pooler"** или **"Connection Pooling"**.

1. В Supabase Dashboard → **Settings** → **Database**
2. Найдите раздел **Connection string**
3. Ищите вкладку **"Session mode"** или **"Connection pooling"**
4. Скопируйте строку подключения оттуда

Она будет выглядеть примерно так:
```
postgresql://postgres.dxfnguweggcefslzgvzs:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true
```

**Обратите внимание:**
- Порт **6543** (не 5432)
- `pooler.supabase.com` в адресе
- `postgres.[PROJECT-REF]` в начале

---

### Шаг 2: Обновите .env файл

Замените текущую строку в файле `.env` на Session Pooler URL:

```
DATABASE_URL=postgresql://postgres.dxfnguweggcefslzgvzs:tQ/5ShuHtg7FbgT@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true
```

**Важно:** Замените `[REGION]` на ваш регион (например, `eu-central-1` или `us-east-1`).

---

### Шаг 3: Перезапустите сервер

После обновления `.env` файла перезапустите сервер.

---

## 🔍 Как найти Session Pooler URL

Если не видите опцию Session Pooler:

1. В разделе **Connection string** попробуйте переключиться между вкладками:
   - **Transaction mode** (для транзакций)
   - **Session mode** (для сессий)
   
2. Оба варианта должны работать с IPv4, но **Session mode** обычно лучше для вашего случая

3. URL будет с портом **6543** и доменом `pooler.supabase.com`

---

## 💡 Почему это работает

- **Session Pooler** использует IPv4-совместимые адреса
- Это стандартный способ подключения к Supabase из внешних приложений
- Не требует покупки IPv4 add-on

---

## 🆘 Если Session Pooler тоже не работает

Тогда можно:
1. Купить **IPv4 add-on** в Supabase (платно)
2. Или использовать Supabase REST API вместо прямого подключения к PostgreSQL

Но обычно Session Pooler решает проблему!
